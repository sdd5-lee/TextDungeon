<?xml version="1.0" encoding="utf-8"?>
<!-- ViewPager2 필수 조건: 최상단은 무조건 match_parent -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="30dp"> <!-- 카드 양옆 여백 -->

    <!-- 실제 눈에 보이는 카드 영역 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="#222222"
        android:orientation="vertical"
        android:padding="20dp"
        android:elevation="4dp">

        <!-- 직업 이미지 -->
        <ImageView
            android:id="@+id/char_image"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:background="#131313"
            android:scaleType="fitCenter" />

        <!-- 직업 이름 -->
        <TextView
            android:id="@+id/char_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:fontFamily="serif"
            android:gravity="center"
            android:textColor="#E9C176"
            android:textSize="24sp"
            android:textStyle="bold"
            android:text="직업 이름" />

        <!-- 가격 정보 (G를 붙여서 직관적으로 표현하려면 Adapter에서 수정해도 됩니다) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#E9C176"
                android:textSize="18sp"
                android:text="💰 " />

            <TextView
                android:id="@+id/char_price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#E9C176"
                android:textSize="18sp"
                android:textStyle="bold"
                android:text="1000" />
        </LinearLayout>

        <!-- 직업 설명 -->
        <TextView
            android:id="@+id/char_desc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:textColor="#A0A0A0"
            android:textSize="14sp"
            android:text="여기에 직업 설명이 들어갑니다." />

        <!-- 구매 버튼 (Java 코드에서 LinearLayout으로 선언되었으므로 동일하게 맞춤) -->
        <LinearLayout
            android:id="@+id/btn_unlock"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:background="#131313"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground"
            android:gravity="center"
            android:padding="16dp">

            <TextView
                android:id="@+id/unlock_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#E9C176"
                android:textSize="16sp"
                android:textStyle="bold"
                android:text="해금하기" />
        </LinearLayout>

    </LinearLayout>
</FrameLayout>